# Copilot Instructions (Draft)

- Reuse existing UI patterns for consistency; if a layout already exists for a similar feature, mirror it instead of inventing a new one.
- When implementing list vs grid views for properties, match the `PropertyCard` `variant` to the current view mode.
- If a request could affect multiple pages or components, ask which scope to apply before making broad UI changes.
