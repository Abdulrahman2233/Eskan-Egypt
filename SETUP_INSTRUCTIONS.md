# 🎯 نظام إدارة موافقات العقارات - دليل البدء السريع

## 📋 ملخص التغييرات

تم تطبيق نظام إدارة موحد لمراجعة وموافقة على العقارات المضافة حديثًا بدلاً من الاعتماد على رسالة موافقة بسيطة.

## ✅ الملفات المعدلة

### Backend (Django)
```
✓ backend/listings/views.py
  └── تحسين endpoint الـ pending مع فلاتر التاريخ والبحث

✓ backend/users/serializers.py
  └── إضافة is_staff و is_superuser للتحقق من الصلاحيات
```

### Frontend (React)
```
✓ src/pages/dashboard/PropertyApprovals.tsx (جديد)
  └── صفحة متخصصة لإدارة العقارات المعلقة

✓ src/components/DashboardLayout.tsx
  └── إضافة رابط "إدارة العقارات" للمسؤولين

✓ src/App.tsx
  └── إضافة الـ route الجديد

✓ src/pages/Auth.tsx
  └── حفظ is_staff و is_superuser عند تسجيل الدخول

✓ src/config.ts
  └── إضافة API_BASE_URL للتوافقية
```

## 🚀 خطوات التثبيت

### 1. Backend Setup
```bash
# لا يوجد حزم إضافية مطلوبة
# جميع الحزم موجودة بالفعل
cd backend
python manage.py migrate  # في حالة وجود migrations جديدة
python manage.py runserver
```

### 2. Frontend Setup
```bash
# لا يوجد حزم إضافية مطلوبة
# جميع المكتبات المستخدمة موجودة
cd ..
npm run dev
# أو
bun run dev
```

## 🔄 سير العمل

### 1️⃣ المستخدم العادي (Landlord)
```
تسجيل الدخول
    ↓
ذهاب للـ Dashboard
    ↓
إضافة عقار جديد
    ↓
تأكيد الإضافة "تم الإضافة بنجاح"
    ↓
العقار في حالة pending
```

### 2️⃣ المسؤول (Admin)
```
تسجيل الدخول (يجب أن يكون is_staff=true)
    ↓
الذهاب إلى Dashboard
    ↓
النقر على "إدارة العقارات" (يظهر للمسؤولين فقط)
    ↓
اختيار الفلتر المطلوب (اليوم/الأسبوع/الشهر/الكل)
    ↓
البحث عن العقار (اختياري)
    ↓
عرض تفاصيل العقار
    ↓
الموافقة ✅ أو الرفض ❌
```

## 🧪 الاختبار

### اختبار إضافة عقار:
1. سجّل دخول كمستخدم عادي
2. اذهب إلى `/dashboard/add-property`
3. ملأ النموذج وأرسله
4. يجب أن ترى رسالة "تم إرسال العقار بنجاح"

### اختبار الموافقة:
1. سجّل دخول كمسؤول
   - في قاعدة البيانات: `is_staff = true`
2. اذهب إلى `/dashboard/property-approvals`
3. يجب أن ترى العقارات المضافة حديثًا
4. انقر على "عرض" لمعاينة التفاصيل
5. اختر "الموافقة" أو "الرفض"

## 🔧 Troubleshooting

### المشكلة: الزر "إدارة العقارات" لا يظهر
**الحل:**
- تأكد من أن المستخدم له `is_staff = true` في قاعدة البيانات
- اتضح من localStorage أن `is_staff` يُحفظ بشكل صحيح

```bash
# في Django shell
from django.contrib.auth.models import User
user = User.objects.get(username='admin')
user.is_staff = True
user.save()
```

### المشكلة: الصفحة تعطي خطأ 403
**الحل:**
- تحقق من أن المستخدم مسجل دخول
- تأكد من أن المستخدم مسؤول (is_staff=true)

### المشكلة: الصور لا تظهر في المعاينة
**الحل:**
- تأكد من أن `MEDIA_URL` و `MEDIA_ROOT` مضبوطة في Django settings
- تحقق من أن الصور تم تحميلها بنجاح في قاعدة البيانات

## 📱 الواجهة

### جدول العقارات
```
┌─────┬──────────────┬────────┬──────────┬──────────┐
│ صورة│ اسم المنطقة │ السعر  │ التاريخ  │ الإجراء  │
├─────┼──────────────┼────────┼──────────┼──────────┤
│ [📷]│ فيلا - الدقي │ 500ك  │ 2026-01-19│ عرض قبول│
│ [📷]│ شقة - النيل │ 350ك  │ 2026-01-18│ عرض قبول│
└─────┴──────────────┴────────┴──────────┴──────────┘
```

### نافذة المعاينة
```
┌────────────────────────────────────────┐
│ اسم العقار                              │
├────────────────────────────────────────┤
│ [صور العقار x 3]                       │
├────────────────────────────────────────┤
│ السعر: 500,000 ريال                    │
│ الغرف: 4                               │
│ الحمامات: 2                            │
│ المساحة: 250 م²                        │
├────────────────────────────────────────┤
│ الوصف:                                 │
│ عقار متميز في موقع استراتيجي...        │
├────────────────────────────────────────┤
│ [✅ الموافقة]  [❌ الرفض]              │
└────────────────────────────────────────┘
```

## 📧 الرسائل البريدية

### عند الموافقة:
```
الموضوع: تم الموافقة على عقارك
المحتوى:
تم الموافقة على العقار [اسم العقار] بنجاح
سيظهر الآن في الموقع
```

### عند الرفض:
```
الموضوع: تم رفض العقار - يتطلب تعديلات
المحتوى:
تم رفض عقارك [اسم العقار]
السبب: [الملاحظات]
يمكنك إعادة التقديم بعد التعديلات
```

## 🔐 الصلاحيات

### المستخدم العادي:
- ✅ إضافة عقارات
- ✅ عرض عقاراته
- ✅ تعديل عقاراته (قبل الموافقة)
- ❌ موافقة على عقارات

### المسؤول:
- ✅ رؤية جميع العقارات المعلقة
- ✅ موافقة على العقارات
- ✅ رفض العقارات مع ملاحظات
- ✅ عرض إحصائيات

## 📊 الإحصائيات

```
اليوم: 5 عقارات
هذا الأسبوع: 12 عقار
إجمالي المعلق: 28 عقار
```

## 🛠️ API Reference

### الحصول على العقارات المعلقة
```bash
GET /api/listings/properties/pending/
Authorization: Bearer {token}

# مع الفلاتر
?filter=today|this_week|this_month|all
&search=query
&ordering=-submitted_at
```

**الرد:**
```json
{
  "count": 5,
  "results": [
    {
      "id": "uuid",
      "name": "اسم العقار",
      "price": 500000,
      "owner": {
        "user": {
          "first_name": "أحمد",
          "email": "ahmed@example.com"
        }
      },
      "submitted_at": "2026-01-19T10:30:00Z",
      ...
    }
  ]
}
```

### الموافقة على عقار
```bash
POST /api/listings/properties/{id}/approve/
Authorization: Bearer {token}
Content-Type: application/json

body:
{
  "approval_notes": "تم قبول العقار"
}
```

### رفض عقار
```bash
POST /api/listings/properties/{id}/reject/
Authorization: Bearer {token}
Content-Type: application/json

body:
{
  "approval_notes": "يتطلب تحسينات في الوصف والصور"
}
```

## 📝 الملاحظات

- تأكد من أن البريد الإلكتروني مُعد بشكل صحيح لإرسال الإخطارات
- النظام يستخدم `submitted_at` لتحديد تاريخ إضافة العقار
- الفلاتر تعتمد على التاريخ الحالي للخادم

## 🎉 الخطوات التالية (مستقبلًا)

- [ ] إضافة تنبيهات فورية (Real-time Notifications)
- [ ] إضافة Bulk Actions (قبول/رفض متعدد)
- [ ] إضافة Reports والإحصائيات المتقدمة
- [ ] إضافة Export (CSV/PDF)
- [ ] إضافة Dashboard متقدم للمسؤولين

## 📞 الدعم

في حالة وجود مشاكل:
1. تحقق من console للأخطاء
2. تحقق من Network tab في DevTools
3. اطلب على Django logs
4. تحقق من قاعدة البيانات
